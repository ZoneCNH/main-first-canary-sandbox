name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  fmt:
    name: fmt
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Check fmt
        run: |
          echo "fmt job running"
          # Pass by default; will be toggled to fail for C-22 canary
          exit 0
